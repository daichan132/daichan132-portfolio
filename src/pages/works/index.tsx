import { Stack, Text } from '@chakra-ui/react'

const Works = () => (
  <Stack spacing={{ base: 2, md: 5 }}>
    <Text textAlign='center' py={10} fontSize='lg'>
      In preparation.
    </Text>
  </Stack>
)

export default Works
